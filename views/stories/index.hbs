<h1>Stories</h1>
<div class="row">
    <!--if I use {{user}}, outside the stories loop, therefore, we will get the user, actual user(logged in user), if we use it inside the stories loop, we will get the user within the stories ie, the writer of the particular story-->
    {{#each stories}}
        <div class="col s12 m4">
            <div class="card">
                <div class="card-image">
                    {{!-- TRIPLE BRACES SINCE WE ARE PARSING HTML, Handlebarse rule --}}
                    {{{editIcon user ../user _id  }}} <!--editIcon takes the following parameters, user(writer of the story), ../user(go out of stories loop and take the logged in user), _id(story id) -->
                </div>
                <div class="card-content center-align">
                    <h5>{{title}}</h5>
                    <p>{{stripTags (truncate body 150)}}</p>
                    <br>
                    <div class="chip">
                        <img src="{{user.image}}" alt="">
                        <a href="/stories/user/{{user._id}}">{{user.displayName}}</a>
                    </div>
                </div>
                <div class="card-action center-align">
                    <a href="/stories/{{_id}}" class="btn grey" >Read More</a>
                </div>
            </div>
        </div>
    {{else}}
        <p>No Stories to display</p>
    {{/each}}
</div>